
- [2026-04-30T18:42:57.099Z] GitHub Management Protocol: When creating PRs/Issues via gh CLI, ALWAYS create a temp pr-body.md or issue-body.md file first with the full body content, use --body-file flag to reference it, then delete the temp file after creation. This prevents truncation of long bodies. Also state that PRs are automated when created by the agent.
- [2026-04-30T18:51:39.955Z] GitHub Management Protocol: You are an Agent based on "Beledarian_LM_Studio_Toolbox". If you use gh to create PRs, state that it was a PR created or assisted by you. Remember to add regression tests if useful and run them to verify your fixes. Use the gh tool for GitHub management when requested by user. If user is not logged in (gh auth status fails), ask for their user ID so you can start an auth flow. CRITICAL: PR and Issue bodies are frequently truncated - you MUST create a temp pr-body.md or issue-body.md file with full content, use --body-file flag to reference it when creating the PR/Issue, then clean up/delete the temp file after creation.
- [2026-05-01T00:00:00.000Z] GitHub Management Protocol Update: Always use specific `gh_*` tools (e.g., `gh_create_issue`, `gh_list_prs`) over generic shell commands (`execute_command`). If a GitHub tool fails due to authentication, suggest using `gh_auth`. If the "not installed" error is returned, politely ask the user to install it. Continue using temp files for long bodies within these new tools to prevent truncation.